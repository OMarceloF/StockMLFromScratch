"""Plotting helpers shared by the notebooks.

Keeping figure code here (rather than inline in notebooks) means every chart in
the README is reproducible from a single function call.

Modules
-------
plots
    Styled matplotlib helpers for convergence curves, fold-by-fold metrics,
    residual diagnostics and ROC curves.
"""
